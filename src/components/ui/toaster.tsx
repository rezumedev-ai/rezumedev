
// This file is now deprecated as we're using sonner directly
// It's kept for compatibility with existing imports
"use client"
export function Toaster() {
  // This is now a no-op component as we're using the sonner Toaster
  return null;
}
